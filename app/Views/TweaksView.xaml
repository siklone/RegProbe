<UserControl x:Class="RegProbe.App.Views.TweaksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:RegProbe.App.ViewModels"
             mc:Ignorable="d"
             Background="#0D0D0D"
             FontFamily="{DynamicResource FontFamilyText}"
             FontSize="12"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#777777"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12,16,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="2,0,0,0">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#141414"/>
                                <Setter Property="Foreground" Value="#DADADA"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Tag}" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#141414"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#C0392B"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#777777"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12,16,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="2,0,0,0">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#141414"/>
                                <Setter Property="Foreground" Value="#DADADA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#141414"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#C0392B"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChipStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#686868"/>
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="BorderBrush" Value="#252525"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1A1A1A"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#353535"/>
                                <Setter Property="Foreground" Value="#F0F0F0"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="True"/>
                                    <Condition Property="Tag" Value="Advanced"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Root" Property="Background" Value="#20170F"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#5A4020"/>
                                <Setter Property="Foreground" Value="#E0AF63"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="True"/>
                                    <Condition Property="Tag" Value="Risky"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Root" Property="Background" Value="#261614"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#5A2A24"/>
                                <Setter Property="Foreground" Value="#D87A70"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="True"/>
                                    <Condition Property="Tag" Value="ClassA"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Root" Property="Background" Value="#241412"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#5A241C"/>
                                <Setter Property="Foreground" Value="#E36A59"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="True"/>
                                    <Condition Property="Tag" Value="ClassB"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Root" Property="Background" Value="#14202A"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#27445D"/>
                                <Setter Property="Foreground" Value="#6FADE8"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#121212"/>
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="BorderBrush" Value="#232323"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="CaretBrush" Value="#E8E8E8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="{TemplateBinding Padding}"/>
                                <TextBlock Margin="10,0,10,0"
                                           VerticalAlignment="Center"
                                           Foreground="#575757"
                                           IsHitTestVisible="False"
                                           Text="{TemplateBinding Tag}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text.Length, RelativeSource={RelativeSource TemplatedParent}}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#343434"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FlatButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="Foreground" Value="#909090"/>
            <Setter Property="BorderBrush" Value="#252525"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="MinWidth" Value="76"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#373737"/>
                                <Setter Property="Foreground" Value="#E8E8E8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ApplyButtonStyle" TargetType="Button" BasedOn="{StaticResource FlatButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding AppliedStatus}" Value="{x:Static viewModels:TweakAppliedStatus.Applied}">
                    <Setter Property="Background" Value="#2B1715"/>
                    <Setter Property="BorderBrush" Value="#C0392B"/>
                    <Setter Property="Foreground" Value="#F0B1A8"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RepairsActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ApplyButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAdvancedRisk}" Value="True">
                    <Setter Property="Background" Value="#20170F"/>
                    <Setter Property="BorderBrush" Value="#5A4020"/>
                    <Setter Property="Foreground" Value="#E0AF63"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsRisky}" Value="True">
                    <Setter Property="Background" Value="#261614"/>
                    <Setter Property="BorderBrush" Value="#8E3A31"/>
                    <Setter Property="Foreground" Value="#E7A096"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TweakListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionEyebrowStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#696969"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="{DynamicResource FontFamilyDisplay}"/>
        </Style>

        <Style x:Key="CountBadgeStyle" TargetType="Border">
            <Setter Property="Background" Value="#141414"/>
            <Setter Property="BorderBrush" Value="#242424"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>

        <Style x:Key="ToolbarLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="#5E5E5E"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="{DynamicResource FontFamilyDisplay}"/>
        </Style>

        <DataTemplate x:Key="TweakRowTemplate" DataType="{x:Type viewModels:TweakItemViewModel}">
            <Border BorderBrush="#191919" BorderThickness="0,0,0,0.5" Padding="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="112"/>
                    </Grid.ColumnDefinitions>

                    <CheckBox Grid.Column="0" Margin="0,20,20,0" VerticalAlignment="Top" IsChecked="{Binding IsSelected, Mode=TwoWay}">
                        <CheckBox.Template>
                            <ControlTemplate TargetType="CheckBox">
                                <Border x:Name="Root" Width="14" Height="14" Background="Transparent" BorderBrush="#343434" BorderThickness="1" CornerRadius="2">
                                    <Path x:Name="Check" Data="M 2,7 L 5,10 L 11,3" Stroke="#F4F4F4" StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Visibility="Collapsed"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Root" Property="Background" Value="#311714"/>
                                        <Setter TargetName="Root" Property="BorderBrush" Value="#C0392B"/>
                                        <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </CheckBox.Template>
                    </CheckBox>

                    <StackPanel Grid.Column="1" Margin="0,16,24,16">
                        <WrapPanel>
                            <TextBlock Text="{Binding Name}" Margin="0,0,12,6" FontSize="15" FontWeight="SemiBold" Foreground="#ECECEC"/>
                            <Border Margin="0,0,6,6" Padding="7,2" CornerRadius="4" Background="#191919" BorderBrush="#2B2B2B" BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#191919"/>
                                        <Setter Property="BorderBrush" Value="#2B2B2B"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AppliedStatus}" Value="{x:Static viewModels:TweakAppliedStatus.Applied}">
                                                <Setter Property="Background" Value="#17311E"/>
                                                <Setter Property="BorderBrush" Value="#2E7D32"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding AppliedStatus}" Value="{x:Static viewModels:TweakAppliedStatus.Error}">
                                                <Setter Property="Background" Value="#261614"/>
                                                <Setter Property="BorderBrush" Value="#5A2A24"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding StatusText}" FontSize="10" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#8C8C8C"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding AppliedStatus}" Value="{x:Static viewModels:TweakAppliedStatus.Applied}">
                                                    <Setter Property="Foreground" Value="#6DC171"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding AppliedStatus}" Value="{x:Static viewModels:TweakAppliedStatus.Error}">
                                                    <Setter Property="Foreground" Value="#D98075"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                            <Border Margin="0,0,6,6" Padding="7,2" CornerRadius="4" Background="#181818" BorderBrush="#2B2B2B" BorderThickness="1" Visibility="{Binding DataContext.ShowContributorEvidenceUi, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#181818"/>
                                        <Setter Property="BorderBrush" Value="#2B2B2B"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EvidenceClassId}" Value="A">
                                                <Setter Property="Background" Value="#241412"/>
                                                <Setter Property="BorderBrush" Value="#5A241C"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding EvidenceClassId}" Value="B">
                                                <Setter Property="Background" Value="#14202A"/>
                                                <Setter Property="BorderBrush" Value="#27445D"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding EvidenceClassBadgeText}" FontSize="10" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#8D8D8D"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding EvidenceClassId}" Value="A">
                                                    <Setter Property="Foreground" Value="#E36A59"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding EvidenceClassId}" Value="B">
                                                    <Setter Property="Foreground" Value="#6FADE8"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </WrapPanel>

                        <TextBlock Text="{Binding FriendlyDescription}" Margin="0,2,0,0" FontSize="11" Foreground="#8A8A8A" TextWrapping="Wrap" MaxWidth="820"/>
                        <TextBlock Text="{Binding ResearchGateMessage}" Margin="0,7,0,0" FontSize="10" Foreground="#33FFFFFF" TextWrapping="Wrap" Visibility="{Binding HasResearchGateMessage, Converter={StaticResource BoolToVisibility}}"/>
                        <TextBlock Text="{Binding RowMetaText}" Margin="0,10,0,0" FontSize="10" Foreground="#666666" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding RepairsRiskHint}"
                                   Margin="0,6,0,0"
                                   FontSize="10"
                                   Foreground="#B78763"
                                   TextWrapping="Wrap"
                                   Visibility="{Binding DataContext.IsMaintenanceWorkspaceSelected, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding DataContext.IsMaintenanceWorkspaceSelected, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True"/>
                                                <Condition Binding="{Binding HasRepairsRiskHint}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding BatchSummaryLine}" Margin="0,5,0,0" FontSize="10" Foreground="#5B5B5B" TextWrapping="Wrap" Visibility="{Binding HasBatchSummaryLine, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,16,0,0" VerticalAlignment="Top" HorizontalAlignment="Right">
                        <Button Content="Revert"
                                Command="{Binding RollbackCommand}"
                                ToolTip="{Binding RollbackActionTooltip}"
                                Style="{StaticResource FlatButtonStyle}"
                                MinWidth="86"
                                Margin="0,0,6,0"
                                Visibility="{Binding DataContext.IsSettingsWorkspaceSelected, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/>
                        <Button Content="{Binding ActionButtonText}"
                                Command="{Binding ApplyCommand}"
                                ToolTip="{Binding PrimaryActionTooltip}"
                                Style="{StaticResource ApplyButtonStyle}"
                                MinWidth="86"
                                Visibility="{Binding DataContext.IsSettingsWorkspaceSelected, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/>
                        <Button Content="{Binding RepairsActionButtonText}"
                                Command="{Binding ApplyCommand}"
                                ToolTip="{Binding PrimaryActionTooltip}"
                                Style="{StaticResource RepairsActionButtonStyle}"
                                MinWidth="86"
                                Visibility="{Binding DataContext.IsMaintenanceWorkspaceSelected, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="#0D0D0D">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#0F0F0F" BorderBrush="#191919" BorderThickness="0,0,1,0">
            <Grid Margin="0,18,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="20,0,16,12">
                    <TextBlock Text="{Binding WorkspaceCategoryHeader}" Style="{StaticResource SectionEyebrowStyle}"/>
                    <TextBlock Text="{Binding SelectedCategoryLabel}" Margin="0,4,0,0" FontSize="16" FontWeight="SemiBold" Foreground="#F4F4F4"/>
                    <TextBlock Text="{Binding SelectedCategoryContext}" Margin="0,4,0,0" FontSize="11" Foreground="#585858"/>
                </StackPanel>

                <Button Grid.Row="1" Command="{Binding ClearCategorySelectionCommand}" Tag="{Binding IsAllCategoriesSelected}" Style="{StaticResource SidebarButtonStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding AllItemsLabel}" FontSize="12"/>
                        <TextBlock Grid.Column="1" Text="{Binding CurrentWorkspaceItemCount}" FontSize="11" Foreground="#474747"/>
                    </Grid>
                </Button>

                <ListBox Grid.Row="2" Margin="0,8,0,0" ItemsSource="{Binding CategoryGroups}" SelectedValuePath="CategoryName" SelectedValue="{Binding SelectedCategoryName, Mode=TwoWay}" Background="Transparent" BorderThickness="0" ItemContainerStyle="{StaticResource SidebarItemStyle}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type viewModels:CategoryGroupViewModel}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding CategoryName}" FontSize="12"/>
                                <TextBlock Grid.Column="1" Text="{Binding CountBadge}" FontSize="11" Foreground="#474747"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#171717" BorderThickness="0,0,0,1" Padding="24,20,28,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.ColumnSpan="2">
                        <TextBlock Text="{Binding CurrentWorkspaceLabel}" FontSize="20" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                        <TextBlock Text="{Binding CurrentWorkspaceDescription}" Margin="0,5,0,0" FontSize="11" Foreground="#707070" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding CurrentWorkspaceModeNote}" Margin="0,7,0,0" FontSize="11" Foreground="#6A6662" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding WorkspaceStatusHint}" Margin="0,8,0,0" FontSize="11" Foreground="#585858" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" BorderBrush="#171717" BorderThickness="0,0,0,1" Padding="24,12,28,10">
                <StackPanel>
                    <TextBlock Text="{Binding ToolbarSectionLabel}" Margin="0,0,0,8" Style="{StaticResource SectionEyebrowStyle}"/>
                    <WrapPanel>
                        <TextBox Width="380" Margin="0,0,12,8" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SearchBoxStyle}" Tag="{Binding SearchPlaceholder}" ToolTip="{Binding SearchPlaceholder}"/>
                        <CheckBox Tag="Safe" IsChecked="{Binding ShowSafe}" Style="{StaticResource ChipStyle}"><TextBlock Text="Safe"/></CheckBox>
                        <CheckBox Tag="Advanced" IsChecked="{Binding ShowAdvanced}" Style="{StaticResource ChipStyle}"><TextBlock Text="Advanced"/></CheckBox>
                        <CheckBox Tag="Risky" IsChecked="{Binding ShowRisky}" Style="{StaticResource ChipStyle}"><TextBlock Text="Risky"/></CheckBox>
                        <CheckBox Tag="Favorites" IsChecked="{Binding ShowFavoritesOnly}" Style="{StaticResource ChipStyle}">
                            <TextBlock>
                                <Run Text="Favorites"/>
                                <Run Text=" "/>
                                <Run Text="{Binding FavoritesCount, Mode=OneWay}"/>
                            </TextBlock>
                        </CheckBox>
                        <CheckBox Tag="ClassA" IsChecked="{Binding ShowClassA}" Style="{StaticResource ChipStyle}" Visibility="{Binding ShowContributorEvidenceUi, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="Class A"/></CheckBox>
                        <CheckBox Tag="ClassB" IsChecked="{Binding ShowClassB}" Style="{StaticResource ChipStyle}" Visibility="{Binding ShowContributorEvidenceUi, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="Class B"/></CheckBox>
                    </WrapPanel>
                    <TextBlock Text="{Binding ToolbarSectionHint}" Margin="0,4,0,0" FontSize="11" Foreground="#5A5A5A" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" BorderBrush="#171717" BorderThickness="0,0,0,1" Padding="24,10,28,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding FilterSummaryLabel}" Style="{StaticResource ToolbarLabelStyle}"/>
                        <TextBlock Text="{Binding FilterSummary}" Margin="0,4,0,0" FontSize="11" Foreground="#6A6A6A"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding InventorySummaryLabel}" HorizontalAlignment="Right" Style="{StaticResource ToolbarLabelStyle}"/>
                        <TextBlock Text="{Binding InventoryStatusMessage}" Margin="0,4,0,0" FontSize="11" Foreground="#6A6A6A" TextAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="3">
                <ListBox ItemsSource="{Binding TweaksView}" ItemTemplate="{StaticResource TweakRowTemplate}" ItemContainerStyle="{StaticResource TweakListItemStyle}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.Style>
                        <Style TargetType="ListBox">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasVisibleTweaks}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.Style>
                </ListBox>

                <Border Padding="24">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasVisibleTweaks}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="{Binding EmptyStateTitle}" FontSize="14" FontWeight="SemiBold" Foreground="#ECECEC" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding EmptyStateDescription}" Margin="0,6,0,0" FontSize="11" Foreground="#666666" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="360"/>
                        <WrapPanel HorizontalAlignment="Center" Margin="0,14,0,0">
                            <Button Content="{Binding EmptyStateActionText}" Command="{Binding ResetFiltersCommand}" Style="{StaticResource FlatButtonStyle}" MinWidth="128" Margin="0,0,8,0" HorizontalAlignment="Center"/>
                            <Button Content="{Binding ClearCategorySelectionText}" Command="{Binding ClearCategorySelectionCommand}" Style="{StaticResource FlatButtonStyle}" MinWidth="164" Visibility="{Binding CanClearCategorySelection, Converter={StaticResource BoolToVisibility}}"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
