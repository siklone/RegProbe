<UserControl x:Class="RegProbe.App.Views.TweaksWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:tweaks="clr-namespace:RegProbe.App.Views.Tweaks"
             x:Name="Root"
             mc:Ignorable="d"
             Background="{DynamicResource ResearchBackgroundBrush}"
             FontFamily="{DynamicResource FontFamilyText}"
             FontSize="12"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/RegProbe.App;component/Resources/TweaksWorkspaceResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{DynamicResource ResearchBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding SecondaryPanelWidth}"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <tweaks:TweaksSecondaryPanel Grid.Column="0"/>

        <Grid Grid.Column="1"
              Margin="18,14,18,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <tweaks:TweakDetailView Grid.Row="0"/>
            <tweaks:PlanLogDrawer Grid.Row="1"
                                  Margin="0,12,0,0"/>
        </Grid>
    </Grid>
</UserControl>
