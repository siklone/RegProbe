# External Evidence Placeholder

Title: Power-control docs-first Procmon boot log

The raw Procmon PML stays out of git. Use the guest-processed summary JSON plus the exact/path hit CSVs in this folder as the committed runtime proof package.
