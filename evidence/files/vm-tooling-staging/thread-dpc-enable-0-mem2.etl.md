# External Evidence Placeholder

Title: Captured evidence

File: `thread-dpc-enable-0-mem2.etl`

The original capture is `.etl` and is not checked in directly. Keep the linked note and the paired text exports instead.

This placeholder keeps the evidence trail repo-friendly without leaking host-local paths.
