# External Evidence Placeholder

Title: Startup delay WPR trace (`missing`)

File: `startup-delay-missing.etl`

The raw ETL is not checked in directly. Keep the paired JSON summaries and the note in `research/notes/startup-delay-wpr-trace-20260326.md`.
