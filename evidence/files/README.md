# Evidence Files

This tree holds normalized repo-tracked artifacts.

Examples:

- Procmon exports
- Ghidra markdown and summary JSON
- imported text captures
- placeholder files for off-repo or missing artifacts

Large raw captures should stay out of git and be referenced through `artifact_refs.release_url`.
