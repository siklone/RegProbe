<UserControl x:Class="WindowsOptimizer.App.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="SectionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#E3E5E8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="24" />
            <Setter Property="Margin" Value="0,0,0,20" />
        </Style>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#2E3440" />
            <Setter Property="Margin" Value="0,0,0,16" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <StackPanel>
                <!-- Header -->
                <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="SemiBold" Foreground="#2E3440" />
                <TextBlock Margin="0,8,0,24"
                           Text="Modern, safe, and reversible Windows optimization tool"
                           Foreground="#4C566A"
                           TextWrapping="Wrap" />

                <!-- Application Info -->
                <Border Style="{StaticResource SectionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Application Information" Style="{StaticResource SectionHeaderStyle}" />

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="Version:" FontWeight="SemiBold" Foreground="#4C566A" Margin="0,0,8,0" />
                            <TextBlock Text="1.0.0" Foreground="#5E81AC" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="Build:" FontWeight="SemiBold" Foreground="#4C566A" Margin="0,0,8,0" />
                            <TextBlock Text="Release" Foreground="#5E81AC" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="Target Framework:" FontWeight="SemiBold" Foreground="#4C566A" Margin="0,0,8,0" />
                            <TextBlock Text=".NET 8.0" Foreground="#5E81AC" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Architecture:" FontWeight="SemiBold" Foreground="#4C566A" Margin="0,0,8,0" />
                            <TextBlock Text="x64" Foreground="#5E81AC" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Features -->
                <Border Style="{StaticResource SectionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Core Features" Style="{StaticResource SectionHeaderStyle}" />

                        <TextBlock Margin="0,0,0,8" Foreground="#4C566A" TextWrapping="Wrap">
                            <Run Text="Registry-based Tweaks" FontWeight="SemiBold" />
                            <LineBreak />
                            <Run Text="Modify Windows registry for system optimizations with full rollback support." />
                        </TextBlock>

                        <TextBlock Margin="0,0,0,8" Foreground="#4C566A" TextWrapping="Wrap">
                            <Run Text="Service Management" FontWeight="SemiBold" />
                            <LineBreak />
                            <Run Text="Control Windows services safely with elevation handling." />
                        </TextBlock>

                        <TextBlock Margin="0,0,0,8" Foreground="#4C566A" TextWrapping="Wrap">
                            <Run Text="Command-based Tweaks" FontWeight="SemiBold" />
                            <LineBreak />
                            <Run Text="Execute system commands with allowlist security and verification." />
                        </TextBlock>

                        <TextBlock Margin="0,0,0,8" Foreground="#4C566A" TextWrapping="Wrap">
                            <Run Text="Discord Integration" FontWeight="SemiBold" />
                            <LineBreak />
                            <Run Text="Receive notifications and automatic patch uploads via Discord webhooks." />
                        </TextBlock>

                        <TextBlock Foreground="#4C566A" TextWrapping="Wrap">
                            <Run Text="Modern UI" FontWeight="SemiBold" />
                            <LineBreak />
                            <Run Text="Built with WPF and Nord color palette, featuring smooth animations." />
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Safety -->
                <Border Style="{StaticResource SectionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Safety &amp; Reliability" Style="{StaticResource SectionHeaderStyle}" />

                        <TextBlock Margin="0,0,0,8" Foreground="#4C566A" TextWrapping="Wrap">
                            All tweaks support Preview, Apply, Verify, and Rollback operations.
                        </TextBlock>

                        <TextBlock Margin="0,0,0,8" Foreground="#4C566A" TextWrapping="Wrap">
                            Changes are logged to CSV files for audit trails and can be reversed safely.
                        </TextBlock>

                        <TextBlock Foreground="#4C566A" TextWrapping="Wrap">
                            Elevated operations are isolated to a separate host process for security.
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Links -->
                <Border Style="{StaticResource SectionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Project Information" Style="{StaticResource SectionHeaderStyle}" />

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Repository:" FontWeight="SemiBold" Foreground="#4C566A" Margin="0,0,8,0" />
                            <TextBlock Text="github.com/siklone/WPF-Windows-optimizer-with-safe-reversible-tweaks" Foreground="#5E81AC" />
                        </StackPanel>

                        <TextBlock Foreground="#4C566A" TextWrapping="Wrap">
                            This is an open-source project built with modern .NET technologies and WPF for a native Windows experience.
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- License -->
                <Border Background="#ECEFF4"
                        BorderBrush="#D8DEE9"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12">
                    <TextBlock Foreground="#4C566A" TextWrapping="Wrap">
                        <Run Text="Windows Optimizer Suite" FontWeight="SemiBold" />
                        <Run Text="- Use at your own risk. Always create a system restore point before applying system tweaks." />
                    </TextBlock>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
