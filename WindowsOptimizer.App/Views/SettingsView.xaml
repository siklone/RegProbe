<UserControl x:Class="WindowsOptimizer.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#E3E5E8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Opacity" Value="0" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="16" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             To="1"
                                             Duration="0:0:0.35" />
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                             To="0"
                                             Duration="0:0:0.35">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Margin="0,8,0,0"
                       Text="Connect services and manage sharing settings."
                       Foreground="#4B4B4B"
                       TextWrapping="Wrap" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="Discord Integration" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Margin="0,6,0,12"
                           Text="Configure a Discord webhook for quick sharing. Auto patch sends will be wired in later."
                           Foreground="#5B606A"
                           TextWrapping="Wrap" />

                <TextBlock Text="Webhook URL" FontWeight="SemiBold" />
                <Grid Margin="0,6,0,12">
                    <TextBox x:Name="WebhookTextBox"
                             Text="{Binding DiscordWebhookUrl, UpdateSourceTrigger=PropertyChanged}"
                             Height="32"
                             Padding="8,4" />
                    <TextBlock Text="https://discord.com/api/webhooks/..."
                               Foreground="#8A8D93"
                               IsHitTestVisible="False"
                               Margin="10,0,0,0"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=WebhookTextBox, TargetNullValue=''}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <CheckBox Content="Auto patch uploads (preview)"
                          IsChecked="{Binding DiscordAutoPatchEnabled}"
                          Margin="0,0,0,12" />

                <StackPanel Orientation="Horizontal">
                    <Button Content="Save"
                            Command="{Binding SaveCommand}"
                            Padding="16,6"
                            Margin="0,0,10,0" />
                    <Button Content="Send test ping"
                            Command="{Binding SendTestCommand}"
                            Padding="16,6" />
                </StackPanel>

                <TextBlock Margin="0,12,0,0"
                           Text="{Binding StatusMessage}"
                           Foreground="#6B7280"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
