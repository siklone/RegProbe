<UserControl x:Class="WindowsOptimizer.App.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:WindowsOptimizer.App.ViewModels"
             mc:Ignorable="d"
             FontFamily="Segoe UI Variable Text, Segoe UI, Segoe UI Emoji"
             FontSize="12">
    <UserControl.Resources>
        <Style x:Key="StartupTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrightestBrush}"/>
        </Style>

        <Style x:Key="StartupSubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedForegroundBrush}"/>
        </Style>

        <Style x:Key="StartupSectionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BackgroundDarkBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
        </Style>

        <Style x:Key="StartupItemCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BackgroundMediumBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource GlowCyanBorderBrush}"/>
                    <Setter Property="Background" Value="{DynamicResource BackgroundLightBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StartupMetaTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedForegroundBrush}"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <Style x:Key="StartupBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="6,1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BackgroundLightBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="StartupActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="MinWidth" Value="96"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

                <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource StartupTitleStyle}"/>
                <TextBlock Text="Manage programs that run automatically when Windows starts"
                           Style="{StaticResource StartupSubtitleStyle}"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource ModernButtonStyle}"
                    Padding="14,6"
                    Margin="12,0,0,0"
                    HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72C;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="14"
                               Margin="0,0,8,0"/>
                    <TextBlock Text="Refresh"
                               FontSize="12"
                               FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </Grid>

                <!-- Startup Items List -->
        <Border Grid.Row="1"
                Style="{StaticResource StartupSectionCardStyle}"
                Padding="0">
            <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" Padding="14,12,14,6">
                <ItemsControl ItemsSource="{Binding StartupItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type viewModels:StartupItemViewModel}">
                            <Border Style="{StaticResource StartupItemCardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Info Section -->
                                    <StackPanel Grid.Column="0">
                                        <DockPanel Margin="0,0,0,6">
                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="{DynamicResource ForegroundBrightestBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       DockPanel.Dock="Left"/>

                                            <TextBlock Text="{Binding Publisher}"
                                                       Style="{StaticResource StartupMetaTextStyle}"
                                                       MaxWidth="220"
                                                       VerticalAlignment="Center"
                                                       DockPanel.Dock="Left"/>
                                        </DockPanel>

                                        <TextBlock Text="{Binding Command}"
                                                   FontSize="10"
                                                   Foreground="{DynamicResource MutedForegroundBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="0,0,0,6"/>

                                        <WrapPanel>
                                            <Border Background="{Binding ImpactColor}"
                                                    Style="{StaticResource StartupBadgeStyle}"
                                                    Margin="0,0,6,0">
                                                <TextBlock Text="{Binding ImpactText}"
                                                           FontSize="10"
                                                           FontWeight="SemiBold"
                                                           Foreground="{DynamicResource ForegroundBrightestBrush}"/>
                                            </Border>

                                            <Border Background="{Binding StatusColor}"
                                                    Style="{StaticResource StartupBadgeStyle}"
                                                    Margin="0,0,6,0">
                                                <TextBlock Text="{Binding StatusText}"
                                                           FontSize="10"
                                                           FontWeight="SemiBold"
                                                           Foreground="{DynamicResource ForegroundBrightestBrush}"/>
                                            </Border>

                                            <TextBlock Text="{Binding Location}"
                                                       Style="{StaticResource StartupMetaTextStyle}"
                                                       Margin="0"/>
                                        </WrapPanel>
                                    </StackPanel>

                                    <!-- Toggle Button -->
                                    <Button Grid.Column="1"
                                            Command="{Binding ToggleCommand}"
                                            Style="{StaticResource StartupActionButtonStyle}"
                                            VerticalAlignment="Center"
                                            Margin="12,0,0,0"
                                            Content="{Binding ButtonText}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

                <!-- Status Bar -->
        <Border Grid.Row="2"
                Background="{DynamicResource BackgroundDarkBrush}"
                BorderBrush="{DynamicResource GlassBorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="12,10"
                Margin="0,14,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Status Icon -->
                <Ellipse Grid.Column="0"
                         Width="10" Height="10"
                         Fill="{DynamicResource AccentBrightCyanBrush}"
                         Margin="0,0,10,0"
                         VerticalAlignment="Center"/>

                <!-- Status Message -->
                <TextBlock Grid.Column="1"
                           Text="{Binding StatusMessage}"
                           FontSize="12"
                           Foreground="{DynamicResource ForegroundBrightestBrush}"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>

