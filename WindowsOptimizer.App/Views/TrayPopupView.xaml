<UserControl x:Class="WindowsOptimizer.App.Views.TrayPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Width="280" Height="180">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Colors.xaml"/>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{DynamicResource BackgroundDarkBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="Windows Optimizer" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundBrightestBrush}"/>
                <TextBlock Text="Hardware details and tweak workspace" FontSize="10"
                           Foreground="{DynamicResource ForegroundLightBrush}"/>
            </StackPanel>

            <Border Grid.Row="1"
                    Background="{DynamicResource BackgroundLightBrush}"
                    CornerRadius="6"
                    Padding="12"
                    VerticalAlignment="Center">
                <TextBlock Text="{Binding ToolTipText}"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource ForegroundBrightBrush}"/>
            </Border>

            <Button Grid.Row="2"
                    Content="Open Dashboard"
                    Command="{Binding ShowWindowCommand}"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    Height="30"
                    Margin="0,12,0,0"/>
        </Grid>
    </Border>
</UserControl>
