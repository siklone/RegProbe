<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Easing Functions -->
    <QuadraticEase x:Key="EaseOut" EasingMode="EaseOut"/>
    <QuadraticEase x:Key="EaseIn" EasingMode="EaseIn"/>
    <QuadraticEase x:Key="EaseInOut" EasingMode="EaseInOut"/>
    <CubicEase x:Key="CubicEaseOut" EasingMode="EaseOut"/>
    <CircleEase x:Key="CircleEaseOut" EasingMode="EaseOut"/>

    <!-- Page Transition Animations -->
    <Storyboard x:Key="FadeInStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="0:0:0.3"
                        EasingFunction="{StaticResource EaseOut}"/>
    </Storyboard>

    <Storyboard x:Key="FadeOutStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="1" To="0"
                        Duration="0:0:0.2"
                        EasingFunction="{StaticResource EaseIn}"/>
    </Storyboard>

    <Storyboard x:Key="SlideInStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                        From="50" To="0"
                        Duration="0:0:0.3"
                        EasingFunction="{StaticResource CubicEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="0:0:0.3"
                        EasingFunction="{StaticResource EaseOut}"/>
    </Storyboard>

    <Storyboard x:Key="SlideOutStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                        From="0" To="-50"
                        Duration="0:0:0.2"
                        EasingFunction="{StaticResource EaseIn}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="1" To="0"
                        Duration="0:0:0.2"
                        EasingFunction="{StaticResource EaseIn}"/>
    </Storyboard>

    <!-- Component Animations -->
    <Storyboard x:Key="CardHoverStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        To="1.02"
                        Duration="0:0:0.15"
                        EasingFunction="{StaticResource EaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        To="1.02"
                        Duration="0:0:0.15"
                        EasingFunction="{StaticResource EaseOut}"/>
    </Storyboard>

    <Storyboard x:Key="CardUnhoverStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        To="1.0"
                        Duration="0:0:0.15"
                        EasingFunction="{StaticResource EaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        To="1.0"
                        Duration="0:0:0.15"
                        EasingFunction="{StaticResource EaseOut}"/>
    </Storyboard>

    <Storyboard x:Key="ButtonPressStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        To="0.98"
                        Duration="0:0:0.08"
                        EasingFunction="{StaticResource EaseIn}"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        To="0.98"
                        Duration="0:0:0.08"
                        EasingFunction="{StaticResource EaseIn}"/>
    </Storyboard>

    <Storyboard x:Key="ButtonReleaseStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        To="1.0"
                        Duration="0:0:0.08"
                        EasingFunction="{StaticResource EaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        To="1.0"
                        Duration="0:0:0.08"
                        EasingFunction="{StaticResource EaseOut}"/>
    </Storyboard>

    <!-- Loading Animations -->
    <Storyboard x:Key="LoadingSpinnerStoryboard" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                        From="0" To="360"
                        Duration="0:0:1.2"
                        RepeatBehavior="Forever"/>
    </Storyboard>

    <Storyboard x:Key="PulseGlowStoryboard" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0.5" To="1.0"
                        Duration="0:0:1"
                        AutoReverse="True"
                        RepeatBehavior="Forever"
                        EasingFunction="{StaticResource EaseInOut}"/>
    </Storyboard>

    <!-- Status Indicator Animations -->
    <Storyboard x:Key="SuccessCheckmarkStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        From="0" To="1"
                        Duration="0:0:0.3"
                        EasingFunction="{StaticResource CircleEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        From="0" To="1"
                        Duration="0:0:0.3"
                        EasingFunction="{StaticResource CircleEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                        From="-180" To="0"
                        Duration="0:0:0.3"
                        EasingFunction="{StaticResource CircleEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="0:0:0.2"
                        EasingFunction="{StaticResource EaseOut}"/>
    </Storyboard>

    <Storyboard x:Key="ErrorShakeStoryboard">
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
            <LinearDoubleKeyFrame Value="0" KeyTime="0:0:0"/>
            <LinearDoubleKeyFrame Value="-10" KeyTime="0:0:0.05"/>
            <LinearDoubleKeyFrame Value="10" KeyTime="0:0:0.1"/>
            <LinearDoubleKeyFrame Value="-10" KeyTime="0:0:0.15"/>
            <LinearDoubleKeyFrame Value="10" KeyTime="0:0:0.2"/>
            <LinearDoubleKeyFrame Value="0" KeyTime="0:0:0.25"/>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

    <Storyboard x:Key="ProgressPulseStoryboard" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0.3" To="1.0"
                        Duration="0:0:0.8"
                        AutoReverse="True"
                        RepeatBehavior="Forever"
                        EasingFunction="{StaticResource EaseInOut}"/>
    </Storyboard>

    <!-- List Item Animations -->
    <Storyboard x:Key="ListItemSlideInStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                        From="30" To="0"
                        Duration="0:0:0.25"
                        EasingFunction="{StaticResource CubicEaseOut}"/>
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="0:0:0.25"
                        EasingFunction="{StaticResource EaseOut}"/>
    </Storyboard>

    <!-- Value Change Fade Animation -->
    <Storyboard x:Key="ValueFadeOutStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="1" To="0"
                        Duration="0:0:0.15"
                        EasingFunction="{StaticResource EaseIn}"/>
    </Storyboard>

    <Storyboard x:Key="ValueFadeInStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="0:0:0.15"
                        EasingFunction="{StaticResource EaseOut}"/>
    </Storyboard>

</ResourceDictionary>
