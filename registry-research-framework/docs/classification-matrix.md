# Classification Matrix

- `A`: official doc, or cross-layer runtime + static / behavior proof
- `B`: strong but still gated or missing a required v3.1 layer
- `E`: retired or dead-flagged after the required checks

Current v3.1 rollout keeps existing classes visible until each record finishes re-audit.
