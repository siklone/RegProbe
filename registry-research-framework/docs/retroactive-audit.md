# Retroactive Audit

The original v3.1 pass re-audited:

1. `power.disable-cpu-idle-states`
2. the current non-official `A` queue

Official `A` records stay locked unless they only need output normalization.
